# 中兴捧月智能代理赛题解题思路优化版

## 0. 优化结论

原方案的核心方向是对的：不要只做一个 Prompt Agent，而要做“界面理解 + 任务规划 + 动作接地 + 输出校验”的工程化 GUI Agent。

但原方案还需要做几处关键优化，否则容易出现“报告很好看，代码不好落地”的问题：

1. **接口细节必须以 `agent_base.py` 和 `test_runner.py` 为准。**
   赛题说明里 `TYPE` 写的是 `content`，但参考代码实际要求 `{"text": "内容"}`。`CLICK`、`SCROLL`、`OPEN`、`COMPLETE` 的参数 key 也必须完全一致，评测器不会做格式适配。

2. **不能把重 OCR、重 GUI grounding 模型作为主路径。**
   提交包总大小限制 20 MB，且提交阶段使用主办方统一模型和 API 配置。最稳妥的主路径应是“官方 VLM + 轻量规则 + 强校验”，OCR、Set-of-Mark、候选框检测只能作为增强项。

3. **本地公开测试集的 `ref.json` 是调参核心。**
   每一步参考动作里给了归一化点击范围、可接受分支、TYPE 正则、OPEN 应用名。方案必须围绕这些信息建立失败分类、动作命中率统计和专项修复。

4. **不要绝对禁止模型输出坐标。**
   理想状态是“模型选元素 ID，程序转坐标”，但当前参考附件没有现成 UI 树。如果没有可靠 UI Parser，主路径应采用混合策略：模型输出规范 JSON，其中可以包含 `target_id`、`target_desc` 和 `point`。程序优先使用元素中心点，没有元素时才使用模型给出的归一化坐标，并经过校验和修正。

5. **Prompt 要围绕单步决策，不要让模型自由发挥长篇推理。**
   评测按每一步 action 判断，模型输出越长，parser 越容易失败。应要求模型只输出 JSON，所有中文解释只进 `reason` 字段，最终由代码返回 `AgentOutput`。

6. **`COMPLETE` 是高风险动作，应由程序二次拦截。**
   模型判断完成后，还要结合最近动作、页面状态、任务目标关键词、是否仍有“搜索/确认/提交/加入购物车”等按钮判断，避免提前结束导致 0 分。

最终推荐方案：

```text
BaseAgent 官方 VLM 调用
        ↓
轻量任务解析和 App 名映射
        ↓
Prompt 生成单步 JSON 决策
        ↓
输出解析和容错修复
        ↓
动作接地与坐标归一化
        ↓
Validator 强制转换为标准 AgentOutput
        ↓
Memory 记录历史，避免重复点击和提前 COMPLETE
```

这套方案比“纯 Prompt 输出动作”稳定，也比“堆复杂本地模型”更符合比赛限制。

---

# 1. 赛题本质

这是一个移动端 GUI 闭环控制任务：

```text
用户指令 + 当前截图
        ↓
判断当前处于任务哪一步
        ↓
选择下一个合法动作
        ↓
返回 AgentOutput(action, parameters)
        ↓
评测器检查动作是否落在参考范围
        ↓
进入下一张截图继续执行
```

评分不是看模型解释是否合理，而是看每个用例能否按正确动作链执行到 `COMPLETE`。

因此最重要的工程目标是：

```text
格式 100% 合法
坐标稳定命中
TYPE 内容准确
OPEN 应用名完全匹配
不重复卡死
不提前 COMPLETE
```

---

# 2. 必须遵守的官方接口

参考代码要求继承 `BaseAgent`，实现：

```python
def act(self, input_data: AgentInput) -> AgentOutput:
    ...
```

`AgentInput` 可用字段：

```python
instruction: str
current_image: PIL.Image
step_count: int
history_messages: list
history_actions: list
extra: dict
```

`AgentOutput` 必须严格使用下面格式：

```python
AgentOutput(action="CLICK", parameters={"point": [x, y]})
AgentOutput(action="SCROLL", parameters={"start_point": [x1, y1], "end_point": [x2, y2]})
AgentOutput(action="TYPE", parameters={"text": "输入内容"})
AgentOutput(action="OPEN", parameters={"app_name": "应用名"})
AgentOutput(action="COMPLETE", parameters={})
```

注意：

| 动作 | 正确参数 key | 常见错误 |
|---|---|---|
| `CLICK` | `point` | 写成 `coord`、`position` |
| `SCROLL` | `start_point`、`end_point` | 写成 `from`、`to` |
| `TYPE` | `text` | 写成 `content` |
| `OPEN` | `app_name` | 写成 `app` |
| `COMPLETE` | `{}` | 带上 `content` |

`test_runner.py` 会直接校验参数 key，不符合就失败。

---

# 3. 评测器机制带来的策略

本地 `ref.json` 中每个状态可能有一个或多个可接受动作。例如同一步可以接受点击搜索框或点击搜索按钮附近区域，只要命中任一分支就进入对应下一状态。

`CLICK` 检查逻辑：

```text
x_min < x < x_max
y_min < y < y_max
```

坐标已经是 0 到 1000 的归一化坐标，不需要再转成真实像素。

`SCROLL` 检查逻辑：

```text
有精确点时检查距离
无精确点时只检查角度
无坐标要求时直接通过
```

`TYPE` 检查逻辑：

```text
普通文本必须完全相等
包含“正则 ”时按正则匹配
```

所以调参时要做三件事：

1. 统计每个公开用例每一步的参考动作和区域。
2. 把失败分成格式错、动作错、坐标错、文本错、提前完成、重复卡住。
3. 针对最高频错误修规则，而不是盲目改 Prompt。

---

# 4. 最终架构

推荐实现一个轻量但完整的工程化 Agent：

```text
src/
├── agent_base.py              # 官方文件，不修改
├── agent.py                   # 核心 Agent
├── requirements.txt
└── utils/
    ├── action_schema.py       # 标准动作、坐标裁剪、AgentOutput 构造
    ├── app_map.py             # 指令到 App 名映射
    ├── memory.py              # 历史动作、重复点击、阶段记录
    ├── prompt_builder.py      # 单步决策 Prompt
    ├── output_parser.py       # JSON/文本容错解析
    ├── validator.py           # 动作合法性和参数校验
    ├── policy.py              # 规则兜底策略
    └── logger.py              # 调试日志
```

不要一开始就引入复杂 OCR 或本地 UI 检测。优先把 `agent.py + output_parser.py + validator.py + memory.py + policy.py` 做稳。

---

# 5. 主流程设计

```python
class Agent(BaseAgent):
    def _initialize(self):
        self.memory = AgentMemory()
        self.app_mapper = AppMapper()
        self.prompt_builder = PromptBuilder()
        self.output_parser = OutputParser()
        self.validator = ActionValidator()
        self.policy = RulePolicy()

    def reset(self):
        self.memory.reset()

    def act(self, input_data):
        # 1. 先走高置信规则
        rule_action = self.policy.try_decide(input_data, self.memory)
        if rule_action is not None:
            final_action = self.validator.validate(rule_action, input_data, self.memory)
            self.memory.update(final_action, input_data)
            return final_action

        # 2. 构造单步 VLM Prompt
        messages = self.prompt_builder.build(input_data, self.memory)

        # 3. 通过官方方法调用模型
        response = self._call_api(messages, temperature=0, top_p=0.7)
        raw = response.choices[0].message.content

        # 4. 解析模型 JSON
        decision = self.output_parser.parse(raw)

        # 5. 转换和校验为 AgentOutput
        final_action = self.validator.validate(decision, input_data, self.memory)
        final_action.raw_output = raw

        # 6. 更新历史
        self.memory.update(final_action, input_data)
        return final_action
```

关键点：

- 必须通过 `self._call_api()` 调模型，不能自己创建客户端或改 API 配置。
- `reset()` 必须清空每个用例的状态，避免上一个用例污染下一个用例。
- `temperature=0` 更适合评测，减少输出波动。
- 规则能确定的动作不要调用模型，减少 Token 和不稳定性。

---

# 6. 规则优先级

## 6.1 第一步优先 OPEN

公开测试用例大多第 0 步是 `OPEN`。如果 `step_count == 1`，且指令中能识别目标 App，应直接返回：

```python
AgentOutput(action="OPEN", parameters={"app_name": app_name})
```

App 名要和官方参考完全一致，例如：

| 指令关键词 | `app_name` |
|---|---|
| 美团、美团外卖 | 美团 |
| 百度地图、地图、导航 | 百度地图 |
| 哔哩哔哩、B站、bilibili | 哔哩哔哩 |
| 抖音 | 抖音 |
| 快手 | 快手 |
| 爱奇艺 | 爱奇艺 |
| 腾讯视频 | 腾讯视频 |
| 芒果 TV、芒果视频 | 芒果TV |
| 喜马拉雅 | 喜马拉雅 |
| 去哪儿、去哪儿旅行、机票、酒店预订 | 去哪儿旅行 |

如果当前已经多次 `OPEN` 同一 App，就不要继续重复 `OPEN`，转入 VLM 判断。

## 6.2 TYPE 必须来自任务槽位

模型容易在 `TYPE` 时多打解释文本。建议先从指令中抽槽位：

```text
App 名
搜索对象
店铺名
商品名
目的地
起点
备注
联系人或地址
```

当模型输出 `TYPE` 时，Validator 要检查：

```text
text 非空
不包含 JSON、Action、Thought 等残留
不包含多余引号和 Markdown
优先使用任务槽位中的原文
```

例如指令是：

```text
去美团外卖购买窑村干锅猪蹄（科技大学店）店铺的干锅排骨，地址选择默认地址
```

可以抽出：

```json
{
  "app": "美团",
  "shop": "窑村干锅猪蹄",
  "dish": "干锅排骨",
  "address_policy": "默认地址"
}
```

后续搜索店铺时优先输入店铺名，进入店铺后再输入商品名。

## 6.3 SCROLL 使用固定模板

找不到目标元素时使用稳定滚动模板：

```python
SCROLL_DOWN = {"start_point": [500, 800], "end_point": [500, 300]}
SCROLL_UP = {"start_point": [500, 300], "end_point": [500, 800]}
```

移动端列表“查看更多”通常使用 `SCROLL_DOWN`，也就是手指从下往上滑，页面内容向下翻。

## 6.4 COMPLETE 必须二次确认

模型输出 `COMPLETE` 后，不要直接返回。必须通过：

```text
最近 1 到 2 步不是 OPEN
不是刚 TYPE 完还没点搜索或确认
历史中已经有关键确认动作
当前页面没有明显的搜索、确认、提交、购买、加入、下一步按钮
任务目标关键词已在页面或历史动作中出现
```

无法确定时宁可继续点击确认按钮或让 VLM 再判断，不要过早 `COMPLETE`。

---

# 7. Prompt 设计

Prompt 只解决“当前截图下一步做什么”，不要要求模型输出比赛格式字符串。模型输出内部 JSON，代码负责转成 `AgentOutput`。

推荐模板：

```text
你是移动端 GUI Agent。请根据用户任务、当前截图、历史动作，选择下一步唯一动作。

用户任务：
{instruction}

当前步数：
{step_count}

历史动作：
{history_actions}

可用动作只能是：
- CLICK: 点击屏幕上的目标位置
- TYPE: 输入文本
- SCROLL: 滚动页面
- OPEN: 打开应用
- COMPLETE: 任务已经完成

输出严格 JSON，不要 Markdown，不要代码块，不要额外解释：
{
  "action": "CLICK | TYPE | SCROLL | OPEN | COMPLETE",
  "point": [x, y],
  "text": "",
  "app_name": "",
  "scroll_direction": "down | up | none",
  "target_desc": "",
  "confidence": 0.0,
  "reason": ""
}

坐标规则：
- point 必须是归一化坐标，范围 0 到 1000。
- 不确定坐标时选择目标控件的中心位置。

决策规则：
- 第一次进入任务，如果需要打开 App，优先 OPEN。
- 需要输入前，必须先确认输入框已经被点击或当前页面正在输入。
- 不要重复点击历史中刚失败的位置。
- 只有任务已经完成时才 COMPLETE。
- 如果目标没有出现在当前屏幕，使用 SCROLL。
```

为了提高 parser 成功率，可以在 Prompt 最后加一句：

```text
最终只允许输出一行 JSON。
```

---

# 8. 输出解析与容错

模型可能输出：

```text
```json
{"action":"CLICK","point":[520,180]}
```
```

或：

```text
Thought: ...
Action: CLICK:[[520, 180]]
```

`OutputParser` 应按顺序兜底：

1. 提取 ```json 代码块中的 JSON。
2. 提取第一个 `{...}` JSON。
3. 解析 `Action: CLICK`、`click(point=...)`、`CLICK:[[x,y]]`。
4. 动作名归一化：`tap`、`press`、`点击` 都转为 `CLICK`。
5. 无法解析时返回低风险兜底动作，例如 `SCROLL_DOWN` 或重新调用模型。

动作修正表：

| 模型输出 | 修正 |
|---|---|
| `tap`、`press`、`点击` | `CLICK` |
| `input`、`输入` | `TYPE` |
| `swipe`、`滑动` | `SCROLL` |
| `launch`、`打开` | `OPEN` |
| `done`、`finish`、`完成` | `COMPLETE` |

---

# 9. Validator 设计

`ActionValidator` 是核心保底层，所有动作返回前都必须经过它。

## 9.1 坐标裁剪

```python
def clamp_point(point):
    x, y = point
    return [
        max(0, min(1000, int(round(x)))),
        max(0, min(1000, int(round(y))))
    ]
```

## 9.2 参数格式修正

```python
if action == "CLICK":
    return AgentOutput("CLICK", {"point": clamp_point(point)})

if action == "SCROLL":
    return AgentOutput("SCROLL", {
        "start_point": clamp_point(start),
        "end_point": clamp_point(end),
    })

if action == "TYPE":
    return AgentOutput("TYPE", {"text": clean_text(text)})

if action == "OPEN":
    return AgentOutput("OPEN", {"app_name": normalize_app_name(app_name)})

if action == "COMPLETE":
    return AgentOutput("COMPLETE", {})
```

## 9.3 重复动作保护

如果连续两到三次点击同一区域：

```text
CLICK [520, 180]
CLICK [522, 176]
CLICK [519, 181]
```

说明可能没有生效或陷入错误页面。下一步应：

```text
优先 SCROLL
或点击明显返回/关闭按钮
或重新 OPEN 目标 App
```

不要继续输出同一个点。

## 9.4 Token 和步数保护

`TestRunner` 默认最大步数 30，并统计 Token。建议：

```text
每步最多一次 VLM 调用
规则命中时不调用模型
历史只传最近 3 到 5 步，不传完整截图历史
模型输出限制为短 JSON
```

---

# 10. Memory 设计

Memory 不需要复杂，只要能支持防重复和阶段判断：

```python
class AgentMemory:
    def reset(self):
        self.actions = []
        self.opened_app = None
        self.last_type_text = None
        self.repeat_click_count = 0
        self.task_slots = {}

    def update(self, output, input_data):
        self.actions.append({
            "step": input_data.step_count,
            "action": output.action,
            "parameters": output.parameters,
        })
```

建议记录：

```text
最近 5 个动作
最近点击点
最近 TYPE 文本
是否已经 OPEN 目标 App
是否已经输入店铺名/商品名/目的地
是否已经点击搜索/确认
```

这些信息能明显减少重复点击和提前 `COMPLETE`。

---

# 11. 轻量 UI 理解策略

原方案提出 OCR、Set-of-Mark、UI Parser，这个方向可以保留，但要调整优先级。

## 11.1 第一版不依赖本地 OCR

原因：

```text
PaddleOCR、EasyOCR、检测模型依赖重
提交包 20 MB 不适合携带模型
评测环境不一定有系统 OCR
安装复杂依赖会增加失败风险
```

第一版用 VLM 直接看图即可，但 Prompt 要要求输出归一化坐标和目标描述。

## 11.2 第二版加入轻量图像规则

可以只用 `PIL` 和 `numpy` 做轻量增强：

```text
获取图片宽高
压缩图片减少 token
识别状态栏和底部导航栏区域
根据常见布局给搜索框、返回键、关闭按钮设置位置先验
```

例如：

```text
返回键常在左上角：[40, 70]
搜索框常在顶部中间：[500, 85]
搜索按钮常在右上角：[900, 85]
底部确认按钮常在：[500, 900]
```

这些位置先验不能替代模型，但可以在模型低置信时兜底。

## 11.3 第三版再考虑 Set-of-Mark

如果时间充足，再做候选区域标注：

```text
用规则生成若干候选框
在截图上画 1、2、3 编号
让 VLM 选择编号
程序用编号中心点输出 CLICK
```

但这属于增强版，不应阻塞基础得分。

---

# 12. 针对公开测试数据的专项策略

公开用例大多是 App 内搜索、选择、填写、确认类任务：

```text
美团外卖购买商品
百度地图搜索或导航
哔哩哔哩搜索视频
抖音/快手搜索或操作
爱奇艺/腾讯视频/芒果 TV 搜索内容
喜马拉雅搜索音频
去哪儿旅行搜索或预订
```

这些任务可归为四类：

| 类型 | 常见动作链 |
|---|---|
| 搜索播放类 | `OPEN -> 点击搜索 -> TYPE -> 点击搜索结果 -> COMPLETE` |
| 外卖购物类 | `OPEN -> 进入外卖 -> 搜店铺 -> 进店 -> 搜商品 -> 加购 -> 确认 -> COMPLETE` |
| 地图导航类 | `OPEN -> 搜地点 -> 选择结果 -> 路线/导航 -> COMPLETE` |
| 表单确认类 | `点击字段 -> TYPE -> 点击确认/提交 -> COMPLETE` |

可以在 `policy.py` 中写轻量任务模板：

```python
if app == "美团" and "外卖" in instruction:
    task_type = "takeaway_order"
elif app == "百度地图":
    task_type = "map_search"
elif app in VIDEO_APPS:
    task_type = "media_search"
else:
    task_type = "generic_search"
```

模板不直接写死每一步坐标，而是给 Prompt 和 Validator 提供上下文：

```text
当前任务更可能需要先搜索店铺，再搜索商品
当前任务更可能需要点击播放/结果项
当前任务更可能需要选择默认地址
```

---

# 13. 本地评测闭环

必须建立一个失败分析流程。

每次运行公开测试后记录：

```json
{
  "case": "step_meituan_onekey_0001",
  "step": 5,
  "instruction": "...",
  "pred_action": "CLICK",
  "pred_params": {"point": [820, 100]},
  "ref_action": ["CLICK", "CLICK"],
  "ref_params": [...],
  "passed": false,
  "error_type": "click_miss"
}
```

失败类型建议：

| 失败类型 | 处理方式 |
|---|---|
| `format_error` | 修 `OutputParser` 和 `Validator` |
| `wrong_action` | 修 Prompt 和任务模板 |
| `click_miss` | 修坐标先验、要求模型点控件中心 |
| `wrong_type` | 修槽位抽取和 `clean_text` |
| `wrong_open` | 修 App 名映射 |
| `early_complete` | 加强 COMPLETE 拦截 |
| `loop` | 加重复动作保护 |
| `popup` | 加弹窗关闭规则 |

每次只针对最高频错误改一类规则，然后重新跑全部公开用例，避免修一个坏三个。

---

# 14. 最小可用版本

先做一个能稳定提交的 MVP：

```text
1. agent.py 继承 BaseAgent
2. 第一步根据指令直接 OPEN 目标 App
3. VLM 输出严格 JSON
4. OutputParser 能解析 JSON 和常见 Action 文本
5. Validator 强制标准 AgentOutput
6. 坐标全部裁剪到 0 到 1000
7. Memory 记录最近动作，防重复点击
8. COMPLETE 二次校验
9. 跑通官方 test_runner.py
```

这个版本不需要 OCR，也不需要复杂 UI Parser。

---

# 15. 增强版本

在 MVP 稳定后，再做这些增强：

1. **任务槽位抽取增强**
   从指令中提取店铺名、商品名、地点、视频名、音乐名、地址策略。

2. **App 专项模板**
   对美团、百度地图、视频类 App 做不同阶段提示。

3. **页面状态粗分类**
   让模型 JSON 里返回 `screen_state`：

   ```json
   {
     "screen_state": "home | search | result | detail | form | popup | done"
   }
   ```

4. **弹窗 Recovery**
   遇到权限、更新、登录、广告、网络异常时，优先点击“允许/同意/稍后/关闭/跳过/重试”等安全按钮。

5. **Set-of-Mark 候选增强**
   只在点击坐标经常偏的页面启用，不作为主路径依赖。

---

# 16. 具体实现优先级

## P0：必须完成

```text
标准 AgentOutput
App 名映射
JSON Prompt
OutputParser
Validator
坐标裁剪
Memory.reset()
公开测试跑通
```

## P1：明显提分

```text
任务槽位抽取
TYPE 文本清洗
重复点击保护
COMPLETE 拦截
错误分类日志
App 任务模板
```

## P2：冲高分

```text
页面状态识别
弹窗 Recovery
Set-of-Mark
候选点 rerank
失败样例自动复盘
消融实验表
```

---

# 17. 文档和设计分写法

最终设计文档建议突出三个创新点：

## 创新点一：标准动作安全层

将模型输出和评测输出解耦：

```text
模型输出内部 JSON
程序解析、修正、校验
最终只返回标准 AgentOutput
```

这样能显著降低格式错误。

## 创新点二：任务模板增强的单步规划

不是让模型每一步自由推理，而是结合任务类型、历史动作、当前截图做受约束决策。

```text
任务槽位 -> App 模板 -> 当前阶段 -> 下一步动作
```

## 创新点三：动作自校验和失败恢复

在返回动作前做：

```text
动作名合法性
参数 key 合法性
坐标范围
TYPE 文本清洗
OPEN 应用名映射
重复点击检测
COMPLETE 二次确认
弹窗恢复
```

这比单纯 Prompt 更符合真实 GUI Agent 的鲁棒性要求。

---

# 18. 报告结构建议

```markdown
# 基于任务模板与动作自校验的移动端 GUI Agent

## 1. 赛题理解
## 2. 系统总体架构
## 3. 官方接口与动作规范
## 4. 任务解析与 App 映射
## 5. 单步 VLM 决策 Prompt
## 6. 输出解析与动作标准化
## 7. Memory 与防死循环机制
## 8. COMPLETE 判定与 Recovery
## 9. 本地评测与失败分析
## 10. 消融实验
## 11. 创新性总结
```

消融实验表：

| 版本 | App 映射 | Parser | Validator | Memory | 模板 | Recovery | 成功率 |
|---|---|---|---|---|---|---|---|
| Baseline | 否 | 弱 | 否 | 否 | 否 | 否 | xx% |
| V1 | 是 | 是 | 是 | 否 | 否 | 否 | xx% |
| V2 | 是 | 是 | 是 | 是 | 是 | 否 | xx% |
| Final | 是 | 是 | 是 | 是 | 是 | 是 | xx% |

---

# 19. 最容易踩的坑

1. `TYPE` 参数写成 `content`，实际必须是 `text`。
2. `OPEN` 参数写成 `app`，实际必须是 `app_name`。
3. `COMPLETE` 带参数，实际应为 `{}`。
4. 点击坐标用真实像素，实际必须是 0 到 1000 归一化坐标。
5. 没有实现 `reset()`，跨用例污染状态。
6. 每一步传完整历史截图，Token 很快超限。
7. 模型输出 Markdown，parser 解析失败。
8. 第一步没有利用 `OPEN`，在桌面图标上盲点。
9. App 名不精确，例如输出“B站”而不是“哔哩哔哩”。
10. 刚看到搜索结果就 `COMPLETE`，但任务还需要点击、加入、确认。
11. 为了 OCR 引入重依赖，提交包和环境稳定性出问题。
12. 只看单步准确率，不看整条任务能否到 `COMPLETE`。

---

# 20. 最终建议

当前最适合比赛的路线不是“复现最先进 GUI Agent 论文”，而是：

```text
官方 VLM 能力
+ 轻量任务模板
+ 稳定 Prompt
+ 强 OutputParser
+ 强 Validator
+ Memory 防循环
+ 本地 ref.json 失败分析
```

一句话概括：

> 让模型负责看图和判断意图，让程序负责格式、坐标、状态、兜底和评分细节。

先把 P0 做到公开测试稳定，再加 P1 和 P2。这样既能提高打榜分，也能在代码质量和设计方案创新分上有明确抓手。
