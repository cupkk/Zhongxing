我重新核查了 `cupkk/Zhongxing` 的关键文件。先给结论：

**你现在的 Agent 已经不是“没优化”，而是进入了“规则补丁边际收益递减”的阶段。**
现在分数不继续涨，核心不是模型不够强，而是系统结构仍然偏向：

> **任务正则抽取 → 规则模板优先 → VLM 单步决策 → Parser 容错 → Validator 硬纠偏**

这套路线可以把公开集做高，但隐藏集只要页面布局、App 流程、按钮位置稍微变化，固定规则就容易失效。

---

# 1. 当前项目工作进展判断

## 1.1 主流程已经比较完整

`agent.py` 的主流程是：

```text
parse_task
→ policy.pre_decide
→ prompt_builder.build
→ _call_api
→ output_parser.parse
→ validator.validate
→ memory.update
```

这说明你已经完成了一个完整的 GUI Agent 闭环：任务解析、规则预判、模型调用、输出解析、动作校验、记忆更新。这个工程主线是正确的。

现在的问题不是“Agent 没有结构”，而是结构里最核心的两层还比较弱：

1. **没有真正的 UI 元素候选层。**
2. **规则和 Validator 仍然大量依赖固定坐标。**

---

## 1.2 `policy.py` 是目前最大双刃剑

`policy.py` 里有大量按 App 和 step 写死的流程，例如美团、抖音、快手、B 站、去哪儿、腾讯视频、喜马拉雅、爱奇艺、百度地图等，每一步直接给固定点位。

这说明你已经做了大量针对公开样例或已知失败样例的优化，这也是你能从早期分数涨到 55.17 左右的重要原因。

但是它现在也成了分数上限的来源。

原因是官方评分是逐步检查参考动作：动作类型必须一致，`CLICK` 必须落在参考框里，某一步错了整个 case 就直接失败。你文档里也明确记录了这个机制。

所以当隐藏集出现同一 App 的轻微 UI 变体时，固定点位会出现这种情况：

```text
语义上点的是“发送”
但坐标没落进 ref 框
→ 本 case 直接 0 分
```

这正是你文档中 `douyin_lp_scene_0` 的问题：之前 `COMPLETE` 错，后来改成 `CLICK [887,916]`，但又因为不在参考框内失败。

---

## 1.3 `validator.py` 已经很努力，但仍是“补丁型修复”

`validator.py` 已经做了很多工程保护：

* action 标准化；
* 坐标裁剪；
* 重复点击检测；
* `COMPLETE` 最小步数判断；
* 评论/评价任务特殊处理；
* 电商评价和社交评论区分；
* 输入评价后修正为点击发送或完成。

但它的问题也很明显：
评论/评价类任务仍然依赖：

```text
刚输入长评价文本
+ App 类别
+ 历史点击位置
+ 固定发送点 [887,916] 或 [500,938]
```

这比完全盲点好，但依然不是成熟 GUI Agent 的做法。

成熟做法应该是：

```text
识别当前页面候选按钮
→ 判断哪个按钮像“发布/发送/提交”
→ 点击候选按钮中心
```

而不是：

```text
根据历史流程猜一个固定发送点
```

---

## 1.4 `prompt_builder.py` 仍是自由文本输出，不够稳

当前 Prompt 要求模型输出两行：

```text
Thought: ...
Action: click(...)
```

并且在 Prompt 中写了大量规则，包括评论、电商评价、社交评论、地图、旅行、酒店、外卖等流程。

这个 Prompt 已经很细，但它仍有三个问题：

第一，输出不是严格 JSON，Parser 需要事后猜。
第二，模型直接输出坐标，容易点偏。
第三，Prompt 规则越来越长，模型注意力会分散。

现在的 `output_parser.py` 虽然兼容 JSON、比赛格式、函数格式、中文动作等多种输出形式，鲁棒性很强，但它本质上是在“兜底模型输出不稳定”，不是从源头解决问题。

---

## 1.5 `memory.py` 太薄，不能支撑长任务状态判断

当前 Memory 主要记录：

```text
actions
opened_app
typed_texts
task_slots
```

并提供最近动作、上一步动作、是否打开过 App、重复点击次数。

这对防止简单死循环有用，但不够支撑比赛后半段提分。你真正需要的是：

```text
当前页面状态
当前任务阶段
上一次失败原因
已点击过的 UI 元素
已尝试过的候选按钮
是否处于输入后待提交状态
是否处于评论/评价/发布流程
```

现在很多问题之所以反复出现，是因为 Agent 没有明确知道自己处于哪个阶段，只能靠 `step_count` 和历史点位猜。

---

# 2. 为什么你“改了很多”但分数没涨

原因可以总结为一句话：

> **你现在的优化大多是在修某个失败 case，而不是修失败机制。**

例如 `douyin_lp_scene_0`：

第一阶段失败：

```text
Step 6 期望 CLICK
Agent 输出 COMPLETE
```

于是你加了规则：输入评论后不要直接 COMPLETE，要点击发送。

第二阶段失败：

```text
Step 6 Agent 输出 CLICK [887,916]
Checker: not in scope
```

于是你又加了规则：某些表单评价改点 `[500,938]`。

这个过程说明你已经非常接近问题，但修法仍然是：

```text
失败日志 → 猜测坐标 → 写死补丁
```

隐藏集继续变时，又会出现：

```text
[500,938] 也不在 scope
或者另一个 App 又需要 COMPLETE
或者某个按钮在右上角
```

因此继续堆坐标，很可能只能小幅涨分，甚至出现“公开集 100%，隐藏集不动”的情况。

---

# 3. 对标成熟 GUI Agent，应该学什么

你提到 Codex、Google、Claude、Flowith，这些系统虽然具体实现不同，但有几个共同点非常值得借鉴。

## 3.1 OpenAI computer-use / Codex 类思路

OpenAI 的 computer-use 工具不是让模型随便输出一段文字，而是让模型在一个工具循环中输出结构化电脑动作，由系统执行后再回传截图。它强调的是：

```text
观察屏幕
→ 生成动作
→ 执行动作
→ 再观察
```

对你这个赛题的启发是：

**不要让模型直接产出最终比赛格式，而是让模型产出内部动作 JSON，再由本地程序转成官方 `AgentOutput`。**

你现在是：

```text
VLM 输出 Action 文本
→ Parser 解析
→ Validator 修
```

建议改成：

```text
VLM 输出严格 JSON
→ ActionCandidate
→ Reranker
→ Validator
→ AgentOutput
```

---

## 3.2 Claude computer-use 思路

Claude 的 computer-use 文档非常强调两件事：

1. **执行前校验动作。**
2. **记录 action log，便于恢复和调试。**

对你来说，最应该借鉴的是：

```text
pre-execution validation
action logging
error recovery
```

你已经有 Validator，但缺少完整 action log。现在仓库里更多是文档性复盘，不是机器可聚合的 JSONL 日志。后续要提分，日志比继续调 Prompt 更重要。

---

## 3.3 Google AndroidWorld / Project Mariner 思路

Google AndroidWorld 这类移动端 Agent benchmark 的重点是：真实 App、动态任务、状态奖励、参数化任务变体。

这对你最大的启发是：

**本地公开集 11/11 不能说明隐藏集能涨。**

你必须构造自己的“伪隐藏集”：

```text
同一任务，换 App
同一 App，换入口
同一评论任务，换成发布/提交/完成三种收尾
同一搜索任务，搜索框在顶部/中部/底部
同一按钮，换位置
```

否则你每次优化都会过拟合官方公开样例。

---

## 3.4 AppAgent 思路

AppAgent 的典型思想是：

```text
探索阶段：先学习 App 页面和操作经验
执行阶段：利用经验完成任务
```

你可以借鉴为轻量版本：

```text
每个 App 建一个 policy profile
但 profile 不直接写死坐标
而是写“页面阶段 + 目标控件语义”
```

比如不要写：

```python
抖音 Step 6: CLICK [887,916]
```

而是写：

```text
抖音评论/评价发布阶段：
优先寻找“发布/发送/提交/完成”候选按钮；
若无文本候选，再按布局先验选择底部主按钮。
```

---

## 3.5 OS-Atlas / SeeClick 思路

这类 GUI grounding 工作的核心是：

> **把“想点哪里”和“点哪个坐标”分开。**

也就是说，模型不应该直接输出：

```text
CLICK [887,916]
```

而应该输出：

```json
{
  "action": "CLICK",
  "target": "发布按钮",
  "target_id": 6
}
```

再由程序把 `target_id=6` 转成坐标。

这正是你现在最缺的一层。

---

# 4. 下一轮提分核心：从“坐标补丁”升级为“候选元素接地”

你现在要做的不是继续大改模型，而是加一个轻量的 `UIElement` 层。

## 4.1 新增模块一：`ui_elements.py`

作用：把屏幕上可能点击的区域统一表示为候选元素。

建议结构：

```python
from dataclasses import dataclass
from typing import Tuple, List

@dataclass
class UIElement:
    element_id: int
    kind: str  # button / input / icon / card / bottom_button / top_button
    bbox: Tuple[int, int, int, int]  # 0-1000 normalized
    text: str = ""
    hint: str = ""
    score: float = 0.0

    @property
    def center(self) -> List[int]:
        x1, y1, x2, y2 = self.bbox
        return [int((x1 + x2) / 2), int((y1 + y2) / 2)]
```

一开始不需要重 OCR，可以先做启发式候选：

```text
顶部搜索框
右上角按钮
中部第一结果卡片
底部输入框
右下角发送按钮
底部居中发布按钮
左上角返回/关闭
```

这已经能解决很多固定点位问题。

---

## 4.2 新增模块二：`candidate_miner.py`

给每一屏生成候选点：

```python
def build_candidates(input_data, memory, task_slots):
    candidates = []

    candidates.append(UIElement(1, "top_search", (160, 35, 840, 130), hint="顶部搜索框"))
    candidates.append(UIElement(2, "top_right", (790, 20, 980, 150), hint="右上角搜索/完成/更多"))
    candidates.append(UIElement(3, "first_card", (80, 160, 920, 340), hint="首个结果/首个卡片"))
    candidates.append(UIElement(4, "middle_button", (300, 420, 700, 620), hint="中部主按钮"))
    candidates.append(UIElement(5, "bottom_input", (80, 780, 760, 940), hint="底部输入框"))
    candidates.append(UIElement(6, "bottom_right_send", (760, 800, 980, 980), hint="右下角发送/发布"))
    candidates.append(UIElement(7, "bottom_center_submit", (300, 860, 700, 990), hint="底部居中发布/提交"))
    candidates.append(UIElement(8, "left_top_back", (0, 20, 140, 150), hint="返回/关闭"))

    return candidates
```

重点是：**以后所有 CLICK 尽量来自候选元素中心点，而不是裸坐标。**

---

## 4.3 修改 Prompt：让模型选择 `target_id`

新 Prompt 不要继续要求：

```text
Action: click(point='<point>500 500</point>')
```

建议变成严格 JSON：

```text
你只能输出严格 JSON，不要输出 Markdown，不要输出解释性文字。

候选元素：
1. top_search: 顶部搜索框，bbox=(160,35,840,130)
2. top_right: 右上角搜索/完成/更多，bbox=(790,20,980,150)
3. first_card: 首个结果卡片，bbox=(80,160,920,340)
4. bottom_input: 底部输入框，bbox=(80,780,760,940)
5. bottom_right_send: 右下角发送/发布，bbox=(760,800,980,980)
6. bottom_center_submit: 底部居中发布/提交，bbox=(300,860,700,990)

输出格式：
{
  "action": "CLICK | TYPE | SCROLL | OPEN | COMPLETE",
  "target_id": 0,
  "text": "",
  "app_name": "",
  "scroll_direction": "down",
  "screen_state": "desktop | home | search | result | detail | form | popup | done | unknown",
  "confidence": 0.0,
  "reason": ""
}
```

然后你的代码做映射：

```python
if decision["action"] == "CLICK" and decision["target_id"]:
    element = candidates[decision["target_id"]]
    point = element.center
```

这样能显著降低 `CLICK not in scope`。

---

# 5. 必须重构的四个模块

## 5.1 `policy.py`：从“主规则”降级为“弱先验”

现在 `policy.pre_decide()` 在第一步打开 App 后，会优先走 `_template_decide()`，很多任务直接被固定流程接管。

建议改成：

```text
P0 高置信规则：只保留 OPEN、明显弹窗、明显死循环恢复。
P1 弱先验规则：作为 prompt hint 或 candidate bias，不直接输出动作。
P2 已知公开样例模板：只在无 API 或 debug 模式使用。
```

也就是说：

```python
pre_decide()
```

只保留：

```text
第一步 OPEN App
连续重复点击后的 SCROLL
无 API 时 fallback
```

其他 App 模板不要直接抢主流程，否则隐藏集一变就过拟合。

---

## 5.2 `validator.py`：从“固定发送点”改成“候选按钮校验”

现在 `_post_review_action()` 里最终还是在 `[887,916]` 和 `[500,938]` 之间切。

建议改成：

```text
如果刚 TYPE 了评论/评价：
1. 如果模型选择了 bottom_center_submit，优先点它；
2. 如果模型选择了 bottom_right_send，检查当前流程是不是普通评论；
3. 如果无法判断，生成两个候选动作交给 reranker；
4. 电商评价是否 COMPLETE，不由 App 名单单独决定，而由 screen_state + instruction + history 决定。
```

核心代码思想：

```python
if just_typed_review:
    candidates = [
        ActionCandidate("CLICK", target_id="bottom_center_submit", reason="form_submit"),
        ActionCandidate("CLICK", target_id="bottom_right_send", reason="comment_send"),
        ActionCandidate("COMPLETE", reason="ecommerce_review_done")
    ]
    return rank_review_finish_action(candidates, memory, task_slots, screen_state)
```

这比硬编码一个点位更稳。

---

## 5.3 `memory.py`：增加任务阶段

现在 Memory 太薄。

建议增加：

```python
self.screen_states = []
self.stage = "unknown"
self.last_candidates = []
self.failed_points = []
self.clicked_element_ids = []
self.pending_after_type = None
```

尤其是 `pending_after_type` 很重要：

```text
刚输入搜索词 → pending_after_type = search_submit
刚输入评论 → pending_after_type = comment_submit
刚输入评价 → pending_after_type = review_submit_or_complete
刚输入地址 → pending_after_type = address_confirm
```

这样 Validator 就不需要靠 App 名猜。

---

## 5.4 `output_parser.py`：保留兼容，但主路径改严格 JSON

`output_parser.py` 已经很健壮，但它现在承担了太多“容错解析”的职责。

建议：

```text
主路径：strict_json_parse
兜底路径：现有 parser
```

也就是：

```python
try:
    decision = strict_json_parse(raw_output)
except:
    decision = legacy_output_parser.parse(raw_output)
```

同时记录：

```text
parse_mode = strict_json / legacy / fallback
```

后续你就能统计：到底有多少失败来自 Parser。

---

# 6. 现在最应该做的提分任务排序

## P0：必须立即做

| 优先级  | 任务               | 目的          |
| ---- | ---------------- | ----------- |
| P0-1 | 加 JSONL 逐步日志     | 不再靠肉眼猜隐藏失败  |
| P0-2 | Prompt 改严格 JSON  | 降低解析不稳定     |
| P0-3 | 新增 UIElement 候选层 | 解决裸坐标不稳     |
| P0-4 | 评论/评价收尾改成状态机     | 解决 55.17 卡点 |
| P0-5 | 固定 App 模板降级      | 减少隐藏集过拟合    |

---

## P1：第二阶段做

| 优先级  | 任务                | 目的                                 |
| ---- | ----------------- | ---------------------------------- |
| P1-1 | 加 Action Reranker | 在 CLICK / SCROLL / COMPLETE 之间二次选择 |
| P1-2 | 扩展 Memory 阶段状态    | 支持长任务                              |
| P1-3 | 弹窗 RecoveryPolicy | 处理权限、广告、更新、登录                      |
| P1-4 | 自建伪隐藏集            | 防止只对公开集有效                          |
| P1-5 | 失败类型自动统计          | 找首错分布                              |

---

## P2：冲奖增强

| 优先级  | 任务               | 目的                 |
| ---- | ---------------- | ------------------ |
| P2-1 | Set-of-Mark 标注图  | 给候选元素编号，让 VLM 选编号  |
| P2-2 | OCR 接入或开发期辅助 OCR | 提升文本按钮识别           |
| P2-3 | App Profile 知识库  | 类似 AppAgent 的轻量经验库 |
| P2-4 | 多候选动作投票          | 对关键步骤增强稳定性         |
| P2-5 | 消融实验和报告图表        | 拿设计方案与代码分          |

---

# 7. 针对 55.17 卡点的专项策略

你当前文档显示，主要卡在评论/评价收尾，尤其是：

```text
输入评价文本后：
到底该 COMPLETE？
还是 CLICK 发送？
如果 CLICK，是右下角还是底部居中？
```

建议不要再写：

```python
if douyin:
    click [500,938]
```

而是写一个显式状态机：

```text
REVIEW_START
→ OPEN_REVIEW_ENTRY
→ SELECT_RATING_OR_OPTION
→ CLICK_TEXTAREA
→ TYPE_REVIEW
→ FINISH_REVIEW
```

`FINISH_REVIEW` 阶段按这个逻辑：

```text
如果页面像普通评论区：
    点 bottom_right_send
如果页面像表单评价页：
    点 bottom_center_submit
如果是电商订单评价且没有明确提交按钮：
    COMPLETE
如果不确定：
    优先 CLICK bottom_center_submit，而不是 COMPLETE
```

原因是官方评分里：

```text
expect CLICK，输出 COMPLETE 必死
expect COMPLETE，输出 CLICK 也必死
```

但从当前失败看，你之前已经经历过 “expect CLICK, got COMPLETE”。说明对评论/评价类，保守策略应该是：

> **输入长评价后，不要轻易 COMPLETE；除非 App 和流程都非常明确是电商评价完成态。**

---

# 8. 推荐的新架构

建议你把项目升级成：

```text
TaskParser
    ↓
CandidateMiner
    ↓
PromptBuilder(strict JSON + candidates)
    ↓
VLM
    ↓
StrictParser
    ↓
ActionCandidate
    ↓
ActionReranker
    ↓
Validator
    ↓
RecoveryPolicy
    ↓
AgentOutput
    ↓
Memory + JSONL Log
```

这套架构对应成熟 GUI Agent 的共性：

```text
观察 → 候选元素 → 规划 → 接地 → 校验 → 执行 → 记录
```

而不是：

```text
观察 → 模型猜坐标 → Validator 修补
```

---

# 9. 可以直接写进仓库的模块清单

建议新增：

```text
submission/src/utils/ui_elements.py
submission/src/utils/candidate_miner.py
submission/src/utils/strict_parser.py
submission/src/utils/action_ranker.py
submission/src/utils/state_machine.py
submission/src/utils/jsonl_logger.py
submission/src/utils/recovery_policy.py
```

建议修改：

```text
submission/src/agent.py
submission/src/utils/prompt_builder.py
submission/src/utils/policy.py
submission/src/utils/validator.py
submission/src/utils/memory.py
submission/src/utils/output_parser.py
```

建议新增开发工具：

```text
tools/run_regression.py
tools/analyze_failures.py
tools/build_submission_manifest.py
```

---

# 10. 具体落地版本：7 天提分计划

## 第 1 天：补日志

先别继续改规则。先让每一步都记录：

```json
{
  "case_id": "...",
  "step": 6,
  "instruction": "...",
  "task_slots": {},
  "recent_actions": [],
  "model_raw": "...",
  "parsed_decision": {},
  "final_action": {},
  "checker_result": "PASS/FAIL",
  "error_type": "click_miss / wrong_action / type_mismatch / early_complete"
}
```

没有这个，后面还是盲修。

---

## 第 2 天：Prompt 改严格 JSON

把 `Thought + Action` 改成严格 JSON。

旧：

```text
Thought: ...
Action: click(...)
```

新：

```json
{
  "action": "CLICK",
  "target_id": 6,
  "text": "",
  "app_name": "",
  "scroll_direction": "none",
  "screen_state": "form",
  "confidence": 0.82,
  "reason": "刚输入评价，需要点击底部发布按钮"
}
```

---

## 第 3 天：加候选元素层

先做启发式候选，不必上复杂 OCR。

核心目标：

```text
让 CLICK 尽量来自 element.center
不要来自模型裸坐标
```

---

## 第 4 天：重构评论/评价状态机

把 `_post_review_action()` 里的固定点位逻辑拆出来，变成：

```text
普通评论
表单评价
电商评价
未知评价
```

每类有不同收尾策略。

---

## 第 5 天：加 Action Reranker

把模型输出、规则建议、恢复建议都变成候选：

```text
model_candidate
policy_candidate
review_finish_candidate
recovery_candidate
```

统一打分选一个。

---

## 第 6 天：构造伪隐藏集

至少构造 30 条：

```text
10 条评论/评价
5 条搜索
5 条地图/出行
5 条视频/收藏/点赞
5 条弹窗/异常
```

每条都至少改一种布局或流程。

---

## 第 7 天：打包、消融、报告

报告里写：

```text
Baseline
+ Strict JSON
+ Candidate Grounding
+ Review State Machine
+ Reranker
+ Recovery
```

这样设计分和代码分都会更好。

---

# 11. 不建议继续做的事

## 不建议 1：继续给每个隐藏失败点写固定坐标

例如：

```python
douyin -> [500,938]
kuaishou -> [887,916]
xhs -> [900,920]
```

这种方法短期可能涨一点，但会很快失效。

---

## 不建议 2：继续把 Prompt 写得越来越长

`prompt_builder.py` 现在规则已经很多。
再继续堆规则，模型未必更稳，反而可能注意力分散。

更好的方式是：

```text
Prompt 短一点
结构更硬一点
候选更明确一点
Validator 更可解释一点
```

---

## 不建议 3：盲目换更大的模型

现在主要瓶颈不是 VLM 不懂，而是：

```text
懂了以后不知道该落在哪个参考框里
```

换模型可能有帮助，但不会根治：

```text
CLICK not in scope
early COMPLETE
wrong action type
```

这些是 Agent 工程问题。

---

# 12. 你下一轮最可能涨分的关键改动

我建议你下一轮只做三个核心改动，不要同时大改太多：

## 改动一：严格 JSON 输出

收益：减少解析不稳。
风险：低。
优先级：最高。

## 改动二：评论/评价收尾状态机

收益：直接针对 55.17 卡点。
风险：中。
优先级：最高。

## 改动三：候选元素层替代裸坐标

收益：减少 `CLICK not in scope`。
风险：中。
优先级：最高。

如果这三项做完，仍然不涨，再看日志里新的第一失败点，不要继续围绕抖音盲改。

---

# 13. 最终判断

你现在的工作进展可以概括为：

```text
已经完成了一个可运行、可提交、公开集表现较好的轻量 GUI Agent。
当前主要瓶颈不是模型能力，而是规则过拟合、坐标接地不稳、缺少候选元素层、缺少结构化日志和任务状态机。
```

下一步提分方向应该从：

```text
写更多 App 模板
修更多固定坐标
堆更长 Prompt
```

转向：

```text
严格 JSON
候选元素 grounding
动作 rerank
评论/评价状态机
结构化日志
伪隐藏集回归
```

最重要的一句话：

> **不要再让 Agent 猜坐标，要让 Agent 选择元素；不要再让 Validator 猜流程，要让 Memory 记录阶段。**

这才是从 55 分继续往上冲的关键。
